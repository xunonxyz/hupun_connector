<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_order_tree_inherit_hupun" model="ir.ui.view">
        <field name="name">sale.order.tree.inherit.hupun</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//tree/header" position="inside">
                <button name="action_sync_hupun_orders" display="always" string="Sync Hupun" type="object"/>
            </xpath>
        </field>
    </record>

</odoo>
