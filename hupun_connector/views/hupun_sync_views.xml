<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_hupun_sync_form" model="ir.ui.view">
        <field name="name">hupun.sync.form</field>
        <field name="model">hupun.sync</field>
        <field name="arch" type="xml">
            <form string="Hupun Synchronization">
                <group>
                    <field name="sync_type"/>
                    <field name="date_start"/>
                    <field name="date_end"/>
                </group>
                <footer>
                    <button name="action_sync" string="Synchronize" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_hupun_sync" model="ir.actions.act_window">
        <field name="name">Hupun Synchronization</field>
        <field name="res_model">hupun.sync</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
    
</odoo>
