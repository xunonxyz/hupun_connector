<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_warehouse_form_inherit_hupun" model="ir.ui.view">
        <field name="name">stock.warehouse.form.inherit.hupun</field>
        <field name="model">stock.warehouse</field>
        <field name="inherit_id" ref="stock.view_warehouse"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button name="action_push_storage_to_hupun" string="Push to Hupun" type="object" class="btn-secondary" invisible="is_hupun_synced"/>
                <button name="action_pull_storage_from_hupun" string="Link from Hupun" type="object" class="btn-secondary" invisible="is_hupun_synced"/>
            </xpath>
            <xpath expr="//notebook" position="inside">
                <page string="Hupun Integration" name="hupun_integration">
                    <group>
                        <field name="hupun_storage_code"/>
                        <field name="is_hupun_synced" readonly="1"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
